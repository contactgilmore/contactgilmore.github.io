# Tokens and Code References

Token implementation is not finalized in Sprint P1.

Expected token categories:

```text
--color-background
--color-surface
--color-text
--color-muted
--color-border
--color-accent
--space-*
--radius-*
--shadow-*
--content-max
--reading-max
```

Prefer CSS custom properties in a small global stylesheet. Avoid framework-specific utility lock-in unless separately approved.
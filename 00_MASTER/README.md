# 00_MASTER

Start here for substantial ContactGilmore Portfolio work.

Read `00_MASTER_DOCTRINE.md`, then the five sprint-system documents, `WHERE_WE_ARE.md`, `ACTIVE_SPRINT.md`, and `LAST_CLOSEOUT_PROMPT.txt`. The active sprint pointer names the detailed sprint record and domain documents required next.

Current sprint records live under `docs/sprints/`. Routine startup does not read `docs/sprints/archive/`; archived records are historical evidence only when explicitly needed.

Do not create duplicate living-state files or duplicate sprint summaries elsewhere.

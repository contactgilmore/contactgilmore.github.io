name: Portfolio governance check

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  governance:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - name: Enforce public hosted-runner isolation
        run: python3 scripts/ci/validate_runner_routing_policy.py
      - name: Validate portfolio governance
        run: python3 scripts/check_governance.py
